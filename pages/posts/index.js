import React from "react";

export default function PostList(props) {
  // data passes though in production mode and data is updated to the sidebar data in edit-mode
  return (
    <h1>PostList loaded successfully</h1>
  );
}
